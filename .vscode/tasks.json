{
    "tasks": [
        {
            "type": "shell",
            "label": "Build test",
            "command": "./build.sh",
            "args": [
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}